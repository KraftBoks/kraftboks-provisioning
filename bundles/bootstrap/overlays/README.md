# HMI overlay images

Place site-specific thermal reference images in this directory. Configure the
matching thermal camera with a browser path such as
`"reference_image": "/overlays/thermal-01.png"` in `config/plant_config.json`.

Do not store credentials or images containing sensitive information here.
